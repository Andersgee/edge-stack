import { BorderWithLabel } from "#src/components/BorderWithLabel";
import { ExamplesList } from "#src/components/ExamplesList";

export default function Page() {
  return (
    <BorderWithLabel label="/examples/page.tsx">
      <ExamplesList />
    </BorderWithLabel>
  );
}
